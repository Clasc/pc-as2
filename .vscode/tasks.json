{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build with open mp",
            "type": "shell",
            "command": "g++ src/*.cpp -fopenmp -o out/bin",
            "group": "build",
        }
    ]
}